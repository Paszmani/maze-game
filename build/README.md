# build/ — recursos de empacotamento (electron-builder)

Coloque aqui o **ícone do app desktop**:

- `build/icon.png` — logo do app, **1024×1024** PNG (mínimo 512×512).

O electron-builder detecta este arquivo automaticamente ao rodar `npm run dist`.
Veja `docs/ICONE.md` para o ícone do Android.
