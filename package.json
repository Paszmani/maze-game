{
  "name": "kiosk-maze",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Jogo de labirinto para totem de evento — core agnostico de engine",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
